# Em dev local, o Vite (porta 3000) já faz proxy automático para localhost:3001
VITE_API_URL=/api/v1

# Em produção, comente a linha acima e descomente a linha abaixo
# VITE_API_URL=https://laranjinha.npx.com.br/api/v1
